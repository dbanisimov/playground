{
  "name": "test",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "sideEffects": true,
  "scripts": {
    "build": "esbuild index.ts --bundle --splitting --outdir=dist --format=esm"
  },
  "dependencies": {
    "firebase": "beta",
    "typescript": "^3.1.6"
  },
  "devDependencies": {
    "esbuild":"0.12.4"
  }
}
